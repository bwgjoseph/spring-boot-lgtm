# MinIO Production Configuration: Distributed mode with SSD persistence
mode: distributed
replicas: 4

# Credential management using existing Kubernetes Secret for security
existingSecret: minio-secret

# Persistence: High-performance SSD volume for object storage durability
persistence:
  enabled: true
  size: 500Gi
  # storageClass: "ssd" # Ensure SSD/NVMe storage class is used

# Create buckets on startup for LGTM stack
buckets:
  - name: loki
    policy: public
  - name: tempo
    policy: public
  - name: mimir
    policy: public

# Resource allocations sized for high-volume S3 metadata caching
resources:
  requests:
    cpu: 500m
    memory: 2Gi
  limits:
    memory: 4Gi

# Service configuration
service:
  type: ClusterIP
  port: 9000

# Console configuration
consoleService:
  type: ClusterIP
  port: 9001
